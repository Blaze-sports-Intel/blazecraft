{
  "name": "blazecraft",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:bridge": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --project tsconfig.json",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.3"
  }
}
